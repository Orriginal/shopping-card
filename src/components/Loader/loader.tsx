import { Loader } from '@mantine/core';

function DotLoader() {
    return (
        <Loader
            color="blue"
            size="xl"
            type="dots"
        />
    );
}

export default DotLoader;
